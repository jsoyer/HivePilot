"""Not collected by pytest (testpaths=["tests"] in pyproject.toml) -- same-
directory marker for the check-test-exists.sh TDD hook, see
test_telegram_channel.py in this directory for the full explanation. The
REAL test suite for `DiscordStreamChannel` lives in
`tests/test_discord_channel.py`.
"""
