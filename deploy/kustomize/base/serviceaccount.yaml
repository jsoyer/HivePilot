# ServiceAccount mounted into the HivePilot API pod. No RBAC bound by
# default (see rbac.yaml, opt-in via overlays/prod) -- see the Helm chart's
# README "RBAC & the kubectl / helm / kustomize runners" for the blast-radius
# discussion before granting this ServiceAccount any RBAC rules.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: hivepilot
