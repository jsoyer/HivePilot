# DEPRECATED — this file is NOT the source of truth for HivePilot dependencies.
# It is kept only for legacy reference and is no longer installed by the
# Dockerfile, CI, Makefile, or scripts/install.sh.
#
# The canonical install path is pyproject.toml's optional-dependency extras:
#   pip install -e ".[dev,notifications]"   # local dev + tests
#   pip install -e ".[notifications]"       # runtime (Docker image)
#   pip install -e ".[full]"                # every integration extra
#
# See pyproject.toml [project.optional-dependencies] for the full extras list.

typer>=0.16,<1.0
rich>=13.7,<14.0
pydantic>=2.8,<3.0
pydantic-settings>=2.3,<3.0
PyYAML>=6.0,<7.0
gitpython>=3.1,<4.0
structlog>=24.1,<25.0
tenacity>=8.2,<9.0
questionary>=2.0,<3.0
ruamel.yaml>=0.18
langchain>=0.2,<0.3
langchain-community>=0.2,<0.3
langgraph>=0.0.59,<1.0
crewai>=0.28,<1.0
textual>=0.47,<1.0
requests>=2.31,<3.0
faiss-cpu>=1.7,<2.0
nltk>=3.8,<4.0
python-dotenv>=1.0,<2.0
fastapi>=0.110,<1.0
uvicorn>=0.24,<1.0
boto3>=1.34,<2.0
itsdangerous>=2.1,<3.0
python-telegram-bot>=20.7,<21.0
prometheus-client>=0.17,<1.0
docker>=7.0,<8.0
