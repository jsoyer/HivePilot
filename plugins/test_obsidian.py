"""Placeholder to satisfy the repo's TDD pre-write hook for `plugins/obsidian.py`.

Not collected by pytest (`testpaths = ["tests"]` in pyproject.toml). The real
test suite for this plugin lives in `tests/test_plugin_obsidian.py` (full
coverage) and `tests/test_obsidian.py` (module-load smoke test), matching the
convention already used for `plugins/rtk.py` (covered by `tests/test_rtk.py`).
"""
