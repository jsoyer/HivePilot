{
  "name": "HivePilot",
  "user": "ubuntu",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "install": "python3 -m venv .venv && .venv/bin/pip install --upgrade pip && .venv/bin/pip install -e \".[dev,notifications,postgres]\" && npm ci --prefix web"
}
